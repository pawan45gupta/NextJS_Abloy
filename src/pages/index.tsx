import type { NextPage } from "next";

import Head from "next/head";

const IndexPage: NextPage = () => {
  return (
    <div>
      <Head>
        <title>EMS price increase</title>
        <section>This is the index page</section>
      </Head>
    </div>
  );
};

export default IndexPage;
